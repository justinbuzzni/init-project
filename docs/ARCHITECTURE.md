# Architecture

> 마지막 갱신: YYYY-MM-DD
> 이 문서는 시스템 구조의 **Single Source of Truth**다. 구조가 바뀌면 **같은 커밋에서** 이 문서를 갱신한다 (코드와 문서의 불일치는 버그).
> ⚠️ 프로젝트 시작 시 아래 placeholder를 실제 내용으로 채우세요. 채우기 전까지는 "아직 정의되지 않음"이 이 문서의 공식 상태입니다.

## 시스템 개요

(이 시스템이 무엇이고 왜 존재하는지 2~4문장. 필요 시 다이어그램.)

## 모듈 구조와 경계

| 모듈/레이어 | 책임 | 위치 |
|------------|------|------|
| (예: api) | HTTP 요청/응답, 입력 검증 | `src/api/` |
| (예: service) | 비즈니스 로직 | `src/service/` |
| (예: repository) | 데이터 접근 | `src/repository/` |

## 의존성 방향

허용되는 import 방향만 명시. 여기 없는 방향은 금지 — 필요하면 먼저 아키텍처 논의.

```
(예: api → service → repository. 역방향 금지. 순환 금지.)
```

## 데이터 저장소와 스키마

- (예: PostgreSQL — 스키마 관리 방식, 마이그레이션 도구)
- (예: 외부 API 의존성)

## 핵심 결정 요약

상세는 ADR에. 여기에는 한 줄 + 링크만.

- [ADR-001](adr/001-example.md) — (결정 한 줄 요약)
