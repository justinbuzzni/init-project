---
기능: [feature-name]
상태: 진행중(Phase 1)
마지막 갱신: YYYY-MM-DD
---

# [기능명] Context

> 목적: 다음 세션의 에이전트가 **이 파일 하나만 읽고 즉시 이어서 작업**할 수 있게 한다.
> 원칙: git log로 알 수 있는 것(무엇)은 최소화. **판단·이유·기각한 대안·함정(왜)**을 기록.
> 프론트매터 `상태` 값: `진행중(Phase N)` | `블록됨` | `완료`. `마지막 갱신`은 갱신할 때마다 오늘 날짜로 (훅이 30일 초과를 감지).

## 현재 상태 (3~5문장)

어디까지 왔고, 지금 무엇을 하는 중인가.

## 핵심 결정 로그 (누적, 최신이 위)

- [YYYY-MM-DD] 결정: X를 선택 / 이유: Y / 기각한 대안: Z / 재검토 조건: W (해당 시 → ADR-NNN)
- [YYYY-MM-DD] ...

## 시도했으나 실패한 접근 ⚠️

재개에 필요한 미해결 제약과 검증된 배움만 기록. 일시적인 환경 오류·해결된 장애를 영구 제약으로 남기지 않는다. 없으면 해당 없음.

- 접근 A → 실패 이유: 관측 근거·현재 유효성·재검토 조건

## 발견된 문제 / 열린 질문

- 코드에서 발견했지만 이번 스코프가 아닌 것 (데드 코드, 잠재 버그 등)
- 사용자 확인이 필요한 것

## 다음 세션 시작점

1. 정확히 무엇부터 시작할지 — `파일경로:함수명` 수준으로
2. 주의사항 (함정, 미완성 부분, 미커밋 파일과 검증 상태)

## 파일 맵

- `path/to/file.py` — 이 기능에서의 역할 한 줄
- `path/to/test_file.py` — ...

## 검증·승인 상태

- 실행한 검증과 결과:
- 미실행 검증과 이유:
- 승인된 범위·근거:
- 남은 승인 대상·재개 조건:
