# AGENTS.md

모든 AI 코딩 에이전트의 공통 운영 정책 원본이다. CLAUDE.md는 이를 참조하고 Claude Code 전용 연결만 설명한다.
사용자의 현재 요청과 이미 부여한 권한을 먼저 확인한다. 이 파일이나 스킬을 수정했다는 이유로 현재 작업의 권한이 늘어나지는 않는다.

## 0. 문서 체계와 적용 범위

| 위치 | 책임 | 읽거나 갱신할 때 |
|------|------|----------------|
| AGENTS.md | 자율성·명확화·승인·완료 정책 | 작업 시작 시 |
| docs/ARCHITECTURE.md | 구조·모듈 경계·의존성 | 코드 변경 전 |
| docs/CONVENTIONS.md | 실제 검증 명령·컨벤션 | 변경·검증 전 |
| docs/adr/ | 장기 설계 결정과 재검토 조건 | 해당 결정에 영향이 있을 때 |
| specs/[feature]/ | spec(요구사항), plan(계획), tasks(작업), context(인수인계) | 해당 기능 작업 시 |
| specs/_templates/ | 기능 문서·ADR 양식 | 대응 문서 생성 시 복사 |
| specs/_archive/ | 완료된 기능 기록 | 유사 작업 배경이 필요할 때 |
| .claude/skills/ | 공통 정책을 사용하는 선택적 절차 | 해당 스킬이 필요할 때 |

- 정책은 여기에, 실행 명령은 CONVENTIONS.md에, 기능별 판단은 해당 specs에 한 번만 쓴다.
- README는 설치·탐색 안내다. 스킬·템플릿·훅은 별도의 승인 조건을 추가하지 않는다.
- 작은 문서 수정·조사·단순 정비는 작업 요약과 검증 결과로 기록한다. 새 기능·여러 세션 작업은 템플릿에서 기능 문서 세트를 만든다.
- 대응 템플릿이 없는 문서는 목적에 맞게 작성한다. 문서 양식 때문에 작업을 중단하지 않는다.
- 승인된 spec의 요구사항은 사용자 지시 없이 바꾸지 않는다.
- 영구 문서는 코드만으로 알기 어려운 이유·제약만 담는다. 결정에는 재검토 조건을 기록한다.

## 1. 판단과 품질

### 1.1 명확화와 자율성

- 먼저 파일·기존 구현·테스트·대화에서 답을 찾는다. 선택이 작고 되돌릴 수 있으며 요청 결과와 권한을 바꾸지 않으면 합리적으로 정하고 필요한 가정을 보고한다.
- 목표·수락 기준이 달라지는 해석, 되돌리기 어려운 선택, 필요한 사용자 정보가 없으면 구체적으로 질문한다.
- 질문이 필요한 부분만 보류한다. 영향을 받지 않는 조사·검증·문서 준비는 계속한다.
- 설명·진행 보고·상태 동기화는 승인 요청과 구분한다. 승인 요청은 대상과 필요한 이유를 명시한다.
- 자율성은 요청 범위 안의 방법 선택에 관한 것이다. 아래 명시적 승인 장치를 생략하는 근거가 아니다.

### 1.2 아키텍처와 승인 경계

- ARCHITECTURE.md의 정의된 모듈 경계와 의존성 방향을 지킨다. 순환 의존성을 만들지 않는다.
- 모듈·외부 의존성·공개 API·데이터 스키마 변경은 실제 영향으로 판단한다. 중요한 책임·의존성 경계 변경, 프레임워크·DB·프로토콜 교체, 호환성 파괴, 데이터 손실 가능성, 보안·운영·비용의 중대한 변화는 plan.md에 영향·대안·검증·복구 방법을 준비하고 승인 후 구현한다.
- 요청 범위 안에서 기존 경계를 지키는 내부 모듈 추가, 호환성을 유지하는 작은 API 추가, 격리된 개발용 도구 추가 등은 영향과 되돌릴 방법을 확인한 뒤 진행하고 기록한다. 운영 DB 변경이나 외부 서비스 도입을 단순 파일·패키지 추가로 축소해서 판단하지 않는다.
- 이미 승인된 계획에 구체적으로 포함된 변경은 같은 이유로 재승인받지 않는다. 새로 발견한 영향이 위 승인 기준에 해당하면 그 부분을 보류하고 변경안을 준비한다. 중대한 영향 여부를 조사로 판단할 수 없으면 질문한다.
- 실제 구조가 바뀌면 같은 변경 단위에서 ARCHITECTURE.md를 갱신한다.
- 되돌리기 비용이 크거나 장기 제약을 만드는 선택은 ADR에 대안·결과·재검토 조건을 기록한다. 일상적인 구현 선택은 필요할 때 context에 짧게 남긴다.
- 아키텍처 문서가 예시 상태면 실제 저장소에서 확인 가능한 사실을 기록한다. 정의되지 않은 시스템 구조를 임의로 확정하지 않는다.

### 1.3 TDD와 Tidy First

- 동작 변경은 가장 작은 의미 있는 실패 테스트 → 최소 구현 → 통과 후 정리 순서로 수행한다.
- 결함은 사용자에게 드러나는 경계에서 재현한다. 별도의 단위 테스트가 원인·회귀를 더 잘 검증할 때만 추가한다. 같은 동작의 테스트 두 개를 기계적으로 만들지 않는다.
- 문서·지침만 바꾸면 링크·형식·정책 일관성 등 산출물에 맞는 검증을 한다.
- 동작을 보존하는 구조 변경과 동작 변경은 별도 논리적 변경/커밋으로 구분한다. 필요한 선행 정리가 있을 때 먼저 수행한다.
- 변경 단위는 검증 가능한 하나의 동작 또는 책임이다. 그 단위를 이루는 여러 파일은 함께 수정할 수 있다.

### 1.4 변경 범위

- 요청을 해결하는 최소한의 변경을 한다. 관련 없는 포맷 변경·리팩토링·기능 추가를 섞지 않는다.
- 기존 스타일을 따르고 새 추상화·의존성은 필요성을 설명할 수 있을 때만 도입한다.
- 본인 변경으로 사용하지 않게 된 코드는 정리한다. 기존의 무관한 문제는 보고만 한다.
- 사용자 변경을 덮어쓰거나 버리지 않는다. 인수인계를 이유로 자동 stash·reset하지 않는다.

## 2. 작업 흐름

### 2.1 컨텍스트 로딩

1. 메모리를 사용할 수 있으면 작업 주제로 회수한다 (§2.8).
2. ARCHITECTURE.md → CONVENTIONS.md → 해당 기능의 spec → plan → tasks → context를 읽는다. 현재 작업과 무관한 기능은 모두 읽을 필요 없다.
3. 해당 기능과 변경에 관련된 ADR을 읽는다.
4. 현재 상태·작업 범위·중요한 가정을 한 문단으로 알리고 진행한다. 근거와 요청이 충돌할 때만 질문한다.

파일이 없거나 예시 상태면 그 사실과 영향을 보고하고 가능한 작업을 계속한다.
훅이 침묵해도 진행 중 기능이 없다고 단정하지 말고 specs를 확인한다.

### 2.2 계획과 명시적 승인

- 사용자가 실행을 요청한 목표 안의 되돌릴 수 있는 구현·검증·정리는 계획을 알리고 진행한다. 일반적인 코드 수정에 별도의 최초 계획 승인을 요구하지 않는다. 검토·계획만 요청했다면 구현하지 않는다.
- 작은 작업은 간결한 실행 계획으로 충분하다. 새 기능·여러 단계 작업은 구현 전 plan/tasks를 작성하거나 갱신한다. 문서 작성 자체를 승인 대기로 연결하지 않는다.
- 작업 순서는 의존성 → 필요한 선행 정리 → 리스크 → 검증 가능성 순으로 정한다. 목표·수락 기준·위험이 같으면 실행 순서를 조정하고 이유를 기록한다.
- 요청 결과를 완성하는 데 필요한 호출부 수정·회귀 테스트 등 세부 작업은 tasks에 추가하고 계속한다. 새 기능·다른 시스템 정비·사용자에게 보이는 요구사항 변경은 범위 확장으로 구분한다.
- 승인된 범위는 Phase를 넘어 완료까지 연속 실행한다. Phase 경계에서는 결과와 다음 작업을 보고한다. 사용자가 명시한 검토·승인 지점에서는 멈춘다.
- 이미 부여된 동일 범위의 승인은 재사용한다. 승인 요청은 아래 대상의 구체적인 변경안을 준비한 뒤 해당 실행 직전에 한다.

| 승인 대상 | 승인 전 준비할 것 |
|-----------|-----------------|
| 중요한 아키텍처 변화·호환성 파괴·데이터 손실 가능성 | §1.2에 따른 영향·대안·검증·복구 방법 |
| 요청 범위 확대·목표 또는 수락 기준 변경 | 기존 요청과의 차이, 필요성, 추가 비용·위험 |
| 에이전트 권한·승인 정책 변경 | 대상 지침의 diff, 늘어나거나 줄어드는 권한 |
| 교훈의 규칙·스킬 승격 | 제안 diff, 적용 범위, 재검토 조건 |
| 사용자가 별도로 지정한 승인 지점 | 요청된 검토 자료와 실행 범위 |

질문·승인이 필요한 부분만 보류하고 독립적인 준비·검증은 계속한다.
일반적인 “계속”을 관련 없는 외부 작업이나 새로운 권한의 허가로 해석하지 않는다.
외부 전송·push·merge·배포 등은 해당 행위에 대한 기존 사용자 권한을 확인하며, 로컬 수정 요청만으로 허가받았다고 보지 않는다.

### 2.3 실행과 완료

- 사용자가 요청하거나 승인한 범위에서 tasks를 한 작업씩 연속 수행한다. 수정 → 관련 검증 → 필요한 문서 동기화 → 완료 체크 순서다.
- 완료한 작업을 짧게 보고하고 다음 작업으로 진행한다. 작업당 커밋을 다음 작업의 필수 전제로 만들지 않는다.
- Phase 완료 시 검증 결과와 context를 갱신한다. 다음 Phase가 남아 있으면 계속 진행한다. §2.2의 승인 대상이나 사용자가 지정한 검토 지점에서만 진행을 보류한다.
- 테스트 실패는 원인을 조사하고 새 증거·검증 가능한 가설이 있으면 해결을 계속한다. 시도 횟수만으로 중단하지 않는다. 새 정보 없이 같은 실패를 반복하거나, 사용자 결정·접근 권한이 필요하거나, 예상 시간·비용·위험이 크게 늘면 증거·시도·대안을 보고하고 영향을 받는 작업을 보류한다. 명시적인 시간·비용 제한은 지킨다.
- 필요한 세부 작업 추가와 순서 조정은 기록하고 계속한다. 범위·요구사항·중대한 아키텍처 영향의 변경은 §2.2에 따라 처리한다.
- 전체 완료는 요구사항 충족, 필요한 검증, 관련 문서 동기화까지다. 검증하지 못한 항목은 완료로 표시하지 않는다.
- 기능 전체 완료 시 장기적으로 남길 결정만 기존 영구 문서에 반영하고 context를 완료로 표기한 뒤 specs/_archive/로 이동한다. 기계적으로 ADR을 새로 만들지 않는다.
- 사용자의 선택·외부 환경 때문에 남은 작업이 있으면 완료한 부분, 미완료 범위, 재개 조건을 구분해 보고한다.

### 2.4 범위 밖 발견

요청과 무관한 문제는 해당 context의 열린 문제 또는 최종 보고에 기록한다.
추가 승인 없이 수정하지 않는다. 기존의 무관한 경고를 모두 고치는 일을 현재 완료 조건으로 확대하지 않는다.

### 2.5 인수인계 문서

- 기존 기능 context를 의미 있는 결정·Phase 경계·종료 시 갱신하고 tasks와 일치시킨다.
- 프론트매터의 기능·상태·마지막 갱신 키를 유지한다. 날짜는 오늘 날짜, 상태는 실제 상태를 쓴다.
- 판단 이유·유효한 제약·다음 시작점·검증 상태·승인 대기 범위를 기록한다.
- 실패 접근은 재개에 필요한 미해결 사실만 기록한다. 해결된 환경 오류를 영구 제약으로 남기지 않는다.
- 오래된 정보는 현재 사실과 구분해 압축한다. 로그·명령 출력 전체를 복제하지 않는다.

### 2.6 종료와 재개

- 사용자가 종료·인수인계를 요청했거나 컨텍스트 유지가 어려우면 안전한 지점에서 상태 기록을 우선한다.
- Phase 경계에서는 §2.3의 상태 동기화 후 계속 진행한다. 경계 자체가 세션 종료·자동 stash·reset의 사유가 되지 않는다.
- 미완성 변경은 작업 트리에 보존하고 파일·검증 상태·다음 단계를 기록한다.
- 커밋은 §4 조건을 충족할 때 수행한다. 커밋 불가를 이유로 인수인계 기록을 생략하지 않는다.
- /handoff는 상태 정리 절차이며 별도의 승인·배포·세션 제어 권한을 주지 않는다.

### 2.7 훅

.claude/settings.json은 세션 시작, 편집, 커밋 후 및 Stop 교훈 리마인더를 연결한다.
훅은 승인 집행기가 아니며 오류도 작업을 차단하지 않도록 설계한다.

- [ACTIVE FEATURES]: 관련 context를 읽고 상태를 보고한 뒤 §2.1에 따라 진행한다.
- [SPEC SYNC]: 관련 문서의 실제 누락을 확인해 동기화한다. 안내만으로 추가 커밋·재승인을 의무화하지 않는다.
- 오래된 갱신 날짜는 검토 신호다. 미완료 기능을 날짜만 보고 완료 또는 아카이브 처리하지 않는다.
- 훅 변경 시 CONVENTIONS.md의 샘플 입력 검증을 수행한다.

### 2.8 메모리

- 메모리는 선택적 보조 수단이다. 사용 가능한 도구를 확인하고 관련 맥락을 회수한다. 호출 실패나 미설치가 작업을 막지 않는다.
- projectPath는 git 메타데이터로 확인한 메인 저장소 절대 경로를 쓴다. worktree 경로로 별도 기억 저장소를 만들지 않는다.
- actor는 현재 에이전트 식별자를 쓴다. 다른 에이전트를 claude-code로 기록하지 않는다.
- 반복 활용 가능한 검증된 해결법만 저장한다. 기존 문서·교훈과 중복이면 저장하지 않는다.
- 비밀, 로그 전체, 일회성 작업 서사, 해결 가능한 환경 장애, 도구가 영구적으로 안 된다는 단정은 저장하지 않는다. 재시도로 해결되면 검증된 해결 패턴만 남긴다.
- 원시 기록 자동 저장은 설치·훅 설정에 달려 있다. 실제 설정을 확인하지 않고 저장됐다고 말하지 않는다.
- 교훈의 규칙·스킬 승격은 §2.2의 승인 대상이다. 단순히 같은 교훈을 검색했다고 반복 적용으로 세지 않는다.
- 재개 지점이 있을 때 체크포인트를 저장한다. 메모리 저장 불가 시 context로 인수인계를 완료한다.

## 3. 검증

CONVENTIONS.md의 실제 명령을 사용한다. 관련 검증은 변경 단위마다, 전체 검증은 코드 커밋 전에 수행한다.
이미 통과한 검증은 추가 변경·실패·새 우려가 있을 때 다시 실행한다.
실행할 테스트가 없는 문서 작업에 가짜 테스트를 만들지 말고 대체 검증과 범위를 명시한다.
환경 문제나 기존 실패 때문에 검증 불가하면 관측 결과와 영향을 보고하고 통과로 표시하지 않는다.

## 4. 커밋

- 관련 변경을 검증하고 필요한 문서를 동기화한 뒤 논리적 단위로 커밋한다.
- 코드 커밋은 전체 테스트와 적용 가능한 린트·타입 검증 통과가 조건이다. 새로 만든 경고를 해결한다.
- 문서 전용 변경은 문서 검증 결과를 사용한다. 미확인 검증을 통과했다고 쓰지 않는다.
- 메시지: [structural|behavioral] 요약 (해당 시 spec/tasks 참조). 구조 변경과 동작 변경을 섞지 않는다.
- 문서 동기화와 tasks 체크는 커밋 전에 한다. 사용자 변경을 함께 스테이징하지 않는다.
- 로컬 커밋은 push·merge·배포·외부 메시지 전송을 허가하지 않는다.
